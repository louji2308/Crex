{
  "name": "crex",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "deploy:worker": "pnpm --filter @crex/worker exec wrangler deploy",
    "migrate:local": "pnpm --filter @crex/worker exec wrangler d1 migrations apply crex --local",
    "migrate:remote": "pnpm --filter @crex/worker exec wrangler d1 migrations apply crex --remote"
  },
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=24.0.0"
  }
}